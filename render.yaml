services:
  - type: web
    name: nationview
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn nationview.wsgi:application"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4
      - key: DEBUG
        value: False
